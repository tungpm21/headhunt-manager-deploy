# 🏥 Audit Report — 2026-04-06

## Summary
- 🔴 Critical Issues: **2** (Thiếu module Submissions + Pipeline sai spec)
- 🟡 Warnings: **3** (Label không đúng, thiếu logic check trùng, thiếu option)
- 🟢 Suggestions: **1** (Đổi tên "Ứng viên" → "Talent Pool")

---

## 🔴 Critical Issues (Phải sửa ngay)

### 1. Thiếu module Submissions trên Menu chính
- **Spec yêu cầu:** Bảng dữ liệu "Submissions" là 1 trong 4 bảng chính, cần menu riêng
- **Thực tế:** Sidebar chỉ có: Tổng quan, Ứng viên, Khách hàng, Job Orders, Nhập dữ liệu
- **Hậu quả:** Không có trang tổng hợp để xem tất cả ứng viên đã gửi cho jobs, phải mở từng Job Order riêng lẻ mới thấy pipeline
- **File:** `src/components/sidebar.tsx` dòng 23-29
- **Cách sửa:** Thêm menu "Submissions" vào `navigation[]`, tạo route `/submissions`

### 2. Pipeline stages không khớp spec Submissions
- **Spec yêu cầu 7 status:** Sent to client → Client reviewing → Interview → Final interview → Offer → Rejected → Hired
- **Thực tế (enum `JobCandidateStage`):** SOURCED → CONTACTED → INTERVIEW → OFFER → PLACED → REJECTED (6 stage, thiếu "Sent to client", "Client reviewing", "Final interview")
- **Hậu quả:** Recruiter không theo dõi được stage chi tiết từ lúc gửi CV cho client đến kết quả cuối
- **File:** `prisma/schema.prisma` dòng 63-71, `src/lib/job-pipeline.ts`

---

## 🟡 Warnings (Nên sửa)

### 3. Nút "Import CRM" nên đổi thành "Import to Talent Pool"
- **Spec (ảnh):** "Import to CRM" phải đổi thành "Import to Talent Pool"
- **Thực tế:** Button ghi "Import CRM" (`import-button.tsx`)
- **File:** `src/app/(dashboard)/moderation/applications/import-button.tsx` dòng 77

### 4. Thiếu option "Import to Talent Pool & Submit" trên Applications
- **Spec (ảnh):** Ngoài "Import to Talent Pool", cần thêm option "Import to Talent Pool & Submit" — vừa lưu UV vào Talent Pool, vừa tạo record Submission cho Job tương ứng
- **Thực tế:** Chỉ có 1 nút "Import CRM" đơn giản

### 5. Thiếu logic check trùng ứng viên dựa trên email/SĐT
- **Spec (ảnh):** Logic check trùng ứng viên dựa trên email hoặc số điện thoại
- **Thực tế:** Import action (`importApplicationToCRM`) có check trùng email nhưng **không check SĐT**
- **File:** `src/lib/moderation-actions.ts`

---

## 🟢 Suggestions (Tùy chọn, theo spec mới)

### 6. Đổi label "Ứng viên" → "Ứng viên (Talent Pool)"
- **Spec:** "Đổi Ứng viên thành Ứng viên (Talent Pool)"
- **File:** `src/components/sidebar.tsx` dòng 25

---

## Bảng so sánh Pipeline Stages: Spec vs Hiện tại

| Spec (Submission Status) | Hiện tại (JobCandidateStage) | Gap |
|--------------------------|------------------------------|-----|
| Sent to client | SOURCED | ❌ Sai nghĩa — SOURCED = đã tìm thấy, chưa gửi |
| Client reviewing | CONTACTED | ❌ Sai nghĩa — CONTACTED = đã liên hệ UV |
| Interview | INTERVIEW | ✅ Khớp |
| Final interview | _(thiếu)_ | ❌ Thiếu hoàn toàn |
| Offer | OFFER | ✅ Khớp |
| Hired | PLACED | ⚠️ Gần giống nhưng tên khác |
| Rejected | REJECTED | ✅ Khớp |
