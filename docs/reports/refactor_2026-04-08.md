# Refactor Report — 2026-04-08

## Mục tiêu
Dọn dẹp dead code (file cũ không còn sử dụng) sau khi redesign Search UI sang inline dropdown.

## Phân tích trước khi sửa

### Nguyên nhân
- Sprint trước đã redesign search từ **fullscreen overlay** sang **inline dropdown** (TopCV-style)
- Logic search mới được viết trực tiếp trong `HeroSection.tsx` và `PublicHeader.tsx`
- 2 file cũ không còn được import ở bất kỳ đâu

### Quét tham chiếu (grep results)

| File | Import từ nơi khác? | Kết luận |
|------|---------------------|----------|
| `SearchOverlay.tsx` | ❌ Không có import nào | Dead code |
| `SearchAutocompleteDropdown.tsx` | ❌ Không có import nào | Dead code |
| `useSearchSuggestions.ts` | ✅ `HeroSection.tsx`, `PublicHeader.tsx` | Đang dùng — giữ lại |

## Thay đổi thực hiện

### ❌ Xóa: `src/components/public/SearchOverlay.tsx` (317 dòng)
- **Chức năng cũ:** Fullscreen search overlay che toàn màn hình
- **Lý do xóa:** Đã thay bằng inline dropdown panels trong HeroSection + PublicHeader
- **Risk:** Không có rủi ro — file không được import ở đâu

### ❌ Xóa: `src/components/public/SearchAutocompleteDropdown.tsx` (180 dòng)
- **Chức năng cũ:** Dropdown autocomplete riêng cho search
- **Lý do xóa:** Logic tương tự đã được inline vào HeroSection + PublicHeader
- **Risk:** Không có rủi ro — file không được import ở đâu

### Tổng cộng: ~497 dòng code thừa đã xóa

## Giữ nguyên (không sửa)

| Item | Lý do |
|------|-------|
| `useSearchSuggestions.ts` | Vẫn dùng bởi HeroSection + PublicHeader |
| SSL `sslmode=require` | Đủ an toàn cho Neon DB, nâng `verify-full` cần CA cert |

## Verification
- `npx tsc --noEmit` → **Exit 0** (zero errors)
- `session.json` updated với record cleanup
- Pending task "Clean up unused SearchOverlay" đã được remove
